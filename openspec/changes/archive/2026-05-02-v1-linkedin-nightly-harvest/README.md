# v1-linkedin-nightly-harvest

Nightly LinkedIn harvest mode with schedule config, strict filters, and inferred post_datetime

Operational helpers for install/remove/run/status/log inspection live as Python entrypoints under `opensignal_job_intel/sources/`.

Cron install helpers capture the absolute Python interpreter path from the environment used at install time.
