## 1. README Updates

- [ ] 1.1 Update "Current Status" to reflect `v1-linkedin-acquisition` capabilities (live mode + filters + stored metadata)
- [ ] 1.2 Update "First-Run Workflow" with both fixture mode and live mode commands
- [ ] 1.3 Document compass `search` configuration (max post age, workplace types, regions)
- [ ] 1.4 Document extraction spec default path under `config/` and local override under `profiles/` (gitignored)
- [ ] 1.5 Document optional local-only env vars: `LINKEDIN_COOKIES`, `LINKEDIN_CSRF`, `LOCAL_LLM_BASE_URL`
- [ ] 1.6 Document what SQLite stores (including location/workplace/post-age fields) and that schema evolves additively

## 2. Changelog

- [ ] 2.1 Add `CHANGELOG.md` with `## [Unreleased]` and an entry documenting the notable `v1-linkedin-acquisition` additions/changes
- [ ] 2.2 Add a short note in README pointing to `CHANGELOG.md`
